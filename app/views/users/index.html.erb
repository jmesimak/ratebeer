<h1>Listing users</h1>

<table class="table table-hover">
  <thead>

  <tr>
    <th>Username</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.username %></td>
    <td><%= link_to 'Show', user %></td>
  </tr>
<% end %>
  </thead>
</table>

<br />

<%= link_to 'New User', new_user_path %>
